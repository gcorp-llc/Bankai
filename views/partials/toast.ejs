<!-- Toast Notification Overlay - GitHub Light Edition -->
<div x-show="toast.show"
     x-cloak
     x-transition:enter="transition ease-out duration-300"
     x-transition:enter-start="opacity-0 transform translate-y-3"
     x-transition:enter-end="opacity-100 transform translate-y-0"
     x-transition:leave="transition ease-in duration-200"
     x-transition:leave-start="opacity-100 transform translate-y-0"
     x-transition:leave-end="opacity-0 transform translate-y-3"
     class="bankai-toast"
     :class="toast.type === 'error' ? 'bankai-toast-error' : 'bankai-toast-success'">
    <template x-if="toast.type !== 'error'">
        <svg class="solar-icon" style="color: #1A7F37;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="9.5" />
            <path d="M8.5 12.5l2.5 2.5 5-5" />
        </svg>
    </template>
    <template x-if="toast.type === 'error'">
        <svg class="solar-icon" style="color: #CF222E;" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 8.5v4.5M12 16.5h.01" />
            <path d="M10.29 3.86L1.82 18a2 2 0 0 0 1.71 3h16.94a2 2 0 0 0 1.71-3L13.71 3.86a2 2 0 0 0-3.42 0z" />
        </svg>
    </template>
    <span x-text="toast.message" style="line-height: 1.4; color: #1F2328;"></span>
</div>
